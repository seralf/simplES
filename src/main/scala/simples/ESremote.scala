package simples

import org.elasticsearch.client.Client
import org.slf4j.LoggerFactory

class ESRemote(client: Client) extends ES(client) {

//  override val logger = LoggerFactory.getLogger(this.getClass)

}
